name: CD
on:
  push:
    branches:
      - main
    tags:
      - 'v*.*.*'

jobs:
  build-and-push-on-ghcr:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: checkout
        uses: actions/checkout@v7


